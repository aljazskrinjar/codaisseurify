
<h1><%= @artist.name %></h1>

<p>Age: <%= @artist.age %></p>
<p>Biography: <%= @artist.bio %></p>
<p>Songs: <% @artist.songs.each do |song| %></p>
<p><%= song.name %>
  <%= link_to "Delete Song", artist_song_path(@artist.id, song.id), method: :delete, data: { confirm: "Are you sure?" } %></p>

<p><% end %></p>
<p><%= image_tag @artist.photo.image %></p>

<%= link_to "Delete Artist", artist_path(@artist.id), method: :delete, data: { confirm: "Are you sure?" } %>


<h1>New song</h1>

<%= render 'songs/form' %>
