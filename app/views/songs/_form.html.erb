<%= form_for [@artist,@artist.songs.new], remote: true, url: artist_songs_path(@artist.id), method: :create do |a| %>
  <%= a.label :name %>
  <%= a.text_field :name %>

  <%= a.submit "Create" %>
<% end %>
